@using KinoAfisha.Models;
@model Format

    <fieldset>
        <legend>Пожалуйста, заполните необходимые поля</legend>

        @using (Html.BeginForm("Edit", "Formats", FormMethod.Post))
        {

            @Html.ValidationSummary();
            @Html.EditorForModel()
            <input type="submit" value="Сохранить" class = "btn btn-outline-success" />
           
            @Html.ActionLink("Отменить", "Index", "Formats", null, new { @class = "btn btn-danger" })
           
        }
    </fieldset>
