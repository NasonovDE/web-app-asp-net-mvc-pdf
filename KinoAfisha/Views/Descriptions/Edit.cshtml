@using KinoAfisha.Models;
@model Description

    <fieldset>
        <legend>Пожалуйста, заполните необходимые поля</legend>
        @using (Html.BeginForm("Edit", "Descriptions", FormMethod.Post))
        {

            @Html.ValidationSummary();
            @Html.EditorForModel()
            <input type="submit" value="Сохранить" class="btn btn-secondary"/>
            @Html.ActionLink("Отменить", "Index", "Descriptions", null, new { @class = "btn btn-danger" })
        }
    </fieldset>
    
    
